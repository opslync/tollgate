{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tollgate.serviceAccountName" . }}
  labels:
    {{- include "tollgate.labels" . | nindent 4 }}
{{- end }}
